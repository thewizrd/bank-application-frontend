import { ApproveAccountRequest } from './approve-account-request';

describe('ApproveAccountRequest', () => {
  it('should create an instance', () => {
    expect(new ApproveAccountRequest()).toBeTruthy();
  });
});
