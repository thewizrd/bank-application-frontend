import { ApproveBeneficiaryRequest } from './approve-beneficiary-request';

describe('ApproveBeneficiaryRequest', () => {
  it('should create an instance', () => {
    expect(new ApproveBeneficiaryRequest()).toBeTruthy();
  });
});
