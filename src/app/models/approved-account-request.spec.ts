import { ApprovedAccountRequest } from './approved-account-request';

describe('ApprovedAccountRequest', () => {
  it('should create an instance', () => {
    expect(new ApprovedAccountRequest()).toBeTruthy();
  });
});
