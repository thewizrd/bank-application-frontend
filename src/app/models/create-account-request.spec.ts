import { CreateAccountRequest } from './create-account-request';

describe('CreateAccountRequest', () => {
  it('should create an instance', () => {
    expect(new CreateAccountRequest()).toBeTruthy();
  });
});
