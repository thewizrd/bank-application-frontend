import { TransferAmountRequest } from './transfer-amount-request';

describe('TransferAmountRequest', () => {
  it('should create an instance', () => {
    expect(new TransferAmountRequest()).toBeTruthy();
  });
});
