import { UpdateCustomerRequest } from './update-customer-request';

describe('UpdateCustomerRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateCustomerRequest()).toBeTruthy();
  });
});
