import { UpdateStaffRequest } from './update-staff-request';

describe('UpdateStaffRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateStaffRequest()).toBeTruthy();
  });
});
